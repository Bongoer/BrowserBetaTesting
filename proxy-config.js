window.POCKET_BROWSER_PROXY = {
  // GitHub Pages has no backend, so Smart Compatibility needs a Wisp relay.
  // Replace this URL with your own Wisp server later if you want full control.
  wisp: "wss://wisp.mercurywork.shop/",
};
